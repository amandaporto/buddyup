<!-- Navigation -->
<nav class="navbar navbar-custom navbar-fixed-top top-nav-collapse" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
        <i class="fa fa-bars"></i>
      </button>
      <a class="navbar-brand page-scroll" href="/">
        <%= image_tag("teamup_shadow.png") %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
      <ul class="nav navbar-nav">
        <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
        <li class="hidden">
          <a href="#page-top"></a>
        </li>

        <!-- Search -->
        <li>
          <%= form_tag(search_users_path, class: "form-inline", id: "search-form") do %>
           Find a TeamMate <span class="glyphicon glyphicon-menu-right"></span><span class="glyphicon glyphicon-menu-right"></span>

            <div class="form-group">
              <% all_activities = Activity.sports.keys %>
              <%= select_tag :sport, options_for_select(all_activities.sort), class:'form-control btn' %>
              <button type="submit" class="btn btn-default-solid"><i class="fa fa-search"></i></button>
            </div>
          <% end %>
        </li>

        <!-- Dropdown -->
        <li>
          <div class="dropdown">
            <button class="btn btn-default-solid dropdown-toggle page-scroll" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
              <i class="fa fa-user fa-fw"></i><%= current_user.name %>
              <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
              <li><%= link_to 'my profile', show_profile_users_path %></li>
              <li><%= link_to 'edit profile', edit_profile_users_path %></li>
              <!-- <li><%= link_to 'invite friends', root_path %></li> -->
              <li>
                <%= link_to 'logout', signout_path, method: :delete %>
              </li>
            </ul>
          </div>
        </li>
      </ul>

    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>
